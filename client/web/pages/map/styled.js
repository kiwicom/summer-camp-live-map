// @flow

import styled from 'styled-components';

// todo - revise if even needed

export const Container = styled.div``;
export const Map = styled.div`
    position: 'absolute';
    top: 0;
    left: 0;
    width: '100%';
    height: '100%';
`;
